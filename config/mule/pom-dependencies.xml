<!-- Add these dependencies to your Mule project's pom.xml -->
<!-- These are required for the JSON layout in log4j2 -->

<dependencies>
    <!-- Log4j2 JSON Layout Support -->
    <dependency>
        <groupId>org.apache.logging.log4j</groupId>
        <artifactId>log4j-core</artifactId>
        <version>2.17.1</version>
    </dependency>

    <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>2.13.0</version>
    </dependency>

    <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-core</artifactId>
        <version>2.13.0</version>
    </dependency>
</dependencies>

<!-- Note: Most Mule 4.x runtimes already include these dependencies.
     Only add them if you encounter ClassNotFoundException errors. -->
