<?xml version="1.0" encoding="utf-8"?>
<Configuration>

    <!--These are some of the loggers you can enable.
        There are several more you can find in the documentation.
        Besides this log4j configuration, you can also use Java VM environment variables
        to enable other logs like network (-Djavax.net.debug=ssl or all) and
        Garbage Collector (-XX:+PrintGC). These will be append to the console, so you will
        see them in the mule_ee.log file. -->

    <Properties>
        <Property name="mule.app.name">demo-elasticsearch</Property>
        <Property name="mule.env">dev</Property>
        <Property name="logstash.host">localhost</Property>
        <Property name="logstash.port">5000</Property>
    </Properties>

    <Appenders>
        <RollingFile name="file" fileName="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}demo-elasticsearch.log"
                 filePattern="${sys:mule.home}${sys:file.separator}logs${sys:file.separator}demo-elasticsearch-%i.log">
            <PatternLayout pattern="%-5p %d [%t] [processor: %X{processorPath}; event: %X{correlationId}] %c: %m%n"/>
            <SizeBasedTriggeringPolicy size="10 MB"/>
            <DefaultRolloverStrategy max="10"/>
        </RollingFile>

        <!-- Socket Appender for Logstash (TCP) -->
        <Socket name="Logstash"
                host="${logstash.host}"
                port="${logstash.port}"
                protocol="TCP"
                reconnectionDelayMillis="5000">
            <JsonLayout compact="true" eventEol="true" properties="true">
                <KeyValuePair key="auth_token" value="${env:LOGSTASH_AUTH_TOKEN:-no-token}"/>
                <KeyValuePair key="tenant_id" value="${ctx:tenant_id:-unknown}"/>
                <KeyValuePair key="application" value="${mule.app.name}"/>
                <KeyValuePair key="environment" value="${mule.env}"/>
                <KeyValuePair key="log_type" value="mule"/>
            </JsonLayout>
        </Socket>

        <!-- Async Appender wrapping Logstash for better performance -->
        <Async name="AsyncLogstash" includeLocation="true">
            <AppenderRef ref="Logstash"/>
        </Async>
    </Appenders>

    <Loggers>
        <!-- Http Logger shows wire traffic on DEBUG -->
        <!--AsyncLogger name="org.mule.service.http.impl.service.HttpMessageLogger" level="DEBUG"/-->
        <AsyncLogger name="org.mule.service.http" level="WARN"/>
        <AsyncLogger name="org.mule.extension.http" level="WARN"/>

        <!-- Mule logger -->
        <AsyncLogger name="org.mule.runtime.core.internal.processor.LoggerMessageProcessor" level="INFO"/>

        <AsyncRoot level="INFO">
            <AppenderRef ref="file"/>
            <AppenderRef ref="AsyncLogstash"/>
        </AsyncRoot>
    </Loggers>

</Configuration>
